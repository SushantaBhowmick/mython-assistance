import { TasksLayout } from "@/components/tasks/TasksLayout";

export default function TasksSectionLayout({ children }: { children: React.ReactNode }) {
  return <TasksLayout>{children}</TasksLayout>;
}
