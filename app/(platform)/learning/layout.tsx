import { LearningLayout } from "@/components/learning/LearningLayout";

export default function LearningSectionLayout({ children }: { children: React.ReactNode }) {
  return <LearningLayout>{children}</LearningLayout>;
}
